---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'fab12f3b-04ad-4fa3-8848-8b0d106405dd'
  PropagateID: 'fab12f3b-04ad-4fa3-8848-8b0d106405dd'
  ReservedCode1: 'bcad57a0-47d8-4c24-94b7-1bd71c625d30'
  ReservedCode2: 'bcad57a0-47d8-4c24-94b7-1bd71c625d30'
---

# 桌面版 vs Web版 功能差异分析报告

> 对比范围：桌面版 `样式保留版_v5.2-修复版.py` (5344行) vs Web版 `excel-ai-assistant-py/` (全部模块)  
> 分析日期：2026-07-25

---

## 一、合并流程差异

### 差异编号：M-01 月份确认验证被跳过

- **桌面版行为**：`_module_02_validate_month_confirmation()` 强制要求用户先点击"更新月份"按钮确认月份（`self.month_confirmed = True`），否则弹出 `messagebox.showerror` 并阻止合并继续
- **Web版行为**：`excel_bridge.py:273` 硬编码 `instance.month_confirmed = True`，直接跳过月份确认验证门控
- **影响**：中 — Web版通过前端月份输入+对话确认流程替代了桌面版的月份确认门控，逻辑上已覆盖但实现路径不同；若前端绕过直接调API则无月份校验
- **建议修复**：在 `/api/merge/execute` 路由中增加月份参数必填校验，month 为空时拒绝执行

### 差异编号：M-02 月份解析能力弱化

- **桌面版行为**：`normalize_month_string()` 支持4种模式 — ①"X月" ②"YYYY-MM"/"YYYY/MM" ③纯数字(含浮点1.0) ④字符串任意位置1-12数字兜底搜索；同时支持 `update_year=True` 自动提取年份
- **Web版行为**：`excel_bridge.py:279-284` 仅用两个简单正则 `r'(\d{1,2})[月\-]?$'` 和 `r'(\d{4})'` 解析月份和年份
- **影响**：低 — 常见格式（"7月"、"2026年7月"、"2026-07"）均能正确解析；但输入"1.0"、"07"等边缘格式时桌面版能处理而Web版可能失败
- **建议修复**：将桌面版 `normalize_month_string()` 提取为独立函数导入Web版，或在 bridge 中实现同等4模式解析

### 差异编号：M-03 文件只读权限未修复

- **桌面版行为**：`ensure_file_writable()` 在保存前检测并修复只读文件权限（`os.chmod(file_path, 0o644)`），微信下载等场景常见此问题
- **Web版行为**：`adjustment.py:617` 直接 `wb.save()`，无只读权限修复；`excel_bridge.py` 委托桌面代码所以合并流程有此保护，但调账流程无
- **影响**：中 — 服务器上传文件通常不会只读，但若用户直接指定服务器已有文件路径则可能保存失败
- **建议修复**：在 `save_adjustment_data()` 保存前调用 `os.chmod(path, 0o644)` 或 try-except 后重试

### 差异编号：M-04 合并前/后文件验证为Web新增

- **桌面版行为**：无合并前后文件完整性校验
- **Web版行为**：`excel_bridge.py` 增加 `_pre_merge_validate()` (文件存在/大小/可读性) 和 `_post_merge_validate()` (输出文件存在/大小/可打开性)
- **影响**：正面增强 — Web版在文件层面增加了更健壮的防御性检查
- **建议修复**：无需修改，保留此增强

### 差异编号：M-05 合并锁机制差异

- **桌面版行为**：用 `self.is_merging` 标志位防止重复触发合并，GUI层自动disable按钮
- **Web版行为**：`excel_bridge.py` 用 `_merge_lock` (threading.Lock) + `_async_merge_lock` (asyncio.Lock) 双层锁；同时硬编码 `instance.is_merging = False` (L274)
- **影响**：低 — Web版锁机制更健壮（线程+异步双保险），硬编码 `is_merging=False` 仅用于绕过桌面版内部检查，不影响并发安全
- **建议修复**：无需修改

### 差异编号：M-06 .xls 格式转换缺失

- **桌面版行为**：`convert_xls_to_xlsx_with_style()` 自动检测 .xls 格式并转换为 .xlsx（保留样式），然后继续后续流程
- **Web版行为**：前端文件上传不限制扩展名，但后端无 .xls→.xlsx 转换逻辑；若上传 .xls 文件，openpyxl 会直接报错
- **影响**：中 — 省公司数据源偶尔仍为 .xls 格式
- **建议修复**：在 upload 或 merge 前增加 .xls 检测，调用 LibreOffice/COM 接口转换，或提示用户先手动转换

---

## 二、调账流程差异

### 差异编号：A-01 地市匹配策略简化

- **桌面版行为**：`match_branch()` 3轮匹配 — ①精确候选(原值/原值+分公司/规范化/规范化+分公司) ②规范化后交叉比较 ③包含关系兜底
- **Web版行为**：`adjustment.py:480-483` 仅4候选列表匹配 `[normalized, normalized+"分公司", city_str, city_str+"分公司"]`，无规范化后交叉比较和包含关系兜底
- **影响**：中 — 大部分地市能精确匹配，但非标准命名（如"长春通信分公司"→需包含关系匹配）会漏匹配
- **建议修复**：在4候选匹配后增加第二轮规范化交叉比较和第三轮包含关系匹配，与桌面版3轮策略对齐

### 差异编号：A-02 年度结算地市匹配策略简化

- **桌面版行为**：`find_matching_city()` 4级匹配 — ①精确 ②规范化精确 ③规范化交叉 ④包含关系
- **Web版行为**：`adjustment.py:589-591` 3条件匹配 — ①精确 `city_str == local_city` ②规范化 `normalized == local_normalized` ③包含关系 `city_str in local_city or local_city in city_str`
- **影响**：低 — 3条件已覆盖大部分场景，缺少的"规范化精确"用例在实际数据中较少出现
- **建议修复**：增加 `normalized == normalize_city_name(local_city)` 规范化精确匹配条件

### 差异编号：A-03 差额数据无兜底计算

- **桌面版行为**：`get_difference_data_from_summary()` 当"差额"列不存在时，回退计算为 应收-应付 的差值
- **Web版行为**：`adjustment.py` 步骤1中直接读取"差额"列，无兜底逻辑
- **影响**：高 — 若汇总表中缺少"差额"列（数据格式变化），Web版调账将完全失败
- **建议修复**：在读取差额列失败时，增加 `差额 = 应收 - 应付` 的兜底计算逻辑

### 差异编号：A-04 公式单元格处理差异

- **桌面版行为**：年度结算表中公式单元格检测后，保留公式仅更新计算值（通过 `month_cell.formula` 读取后重设）
- **Web版行为**：`adjustment.py:603` 直接跳过公式单元格（`if month_cell.data_type != 'f'`），不覆盖公式单元格
- **影响**：中 — 桌面版会更新公式单元格的值（保留公式），Web版则完全不修改公式单元格。若年度结算表当月列含公式，Web版将无法正确更新该行数据
- **建议修复**：对公式单元格，保留公式内容但更新 `.value` 属性为计算结果值，与桌面版行为一致

---

## 三、数据处理差异

### 差异编号：D-01 汇总表标题未更新

- **桌面版行为**：`calculate_receivable_payable_summary()` 更新汇总表的标题，将年月信息写入表头
- **Web版行为**：`adjustment.py` 步骤1-2 更新汇总数据但不更新标题行
- **影响**：低 — 不影响数值正确性，但打开文件时表头可能显示旧月份
- **建议修复**：在汇总表修改后增加标题更新逻辑

### 差异编号：D-02 日志详细度差异

- **桌面版行为**：有 `_log_unmatched_circuits()` 和 `_log_update_summary()` 详细记录未匹配电路和更新摘要
- **Web版行为**：调账模块仅有简单计数统计（`changed_count`），无未匹配项的详细日志
- **影响**：低 — 不影响功能正确性，但排障时缺少详细线索
- **建议修复**：在返回结果中增加 `warnings` 字段，包含未匹配地市列表

### 差异编号：D-03 调账汇总统计弹窗缺失

- **桌面版行为**：`calculate_summary()` 弹出详细统计窗口，展示调账条数、影响金额、各步骤明细
- **Web版行为**：仅返回 `changed_count` 数字，无分步骤明细
- **影响**：低 — 功能上通过API返回值可部分补偿，但用户无法直观看到各步骤影响
- **建议修复**：在返回结果中增加分步骤明细（`summary_changes`, `local_changes`, `annual_changes`）

---

## 四、UI/交互差异

### 差异编号：U-01 合并日志流式展示方式不同

- **桌面版行为**：ScrolledText 实时追加日志，同步阻塞UI
- **Web版行为**：SSE (Server-Sent Events) 流式推送日志到前端，`index.html:1456-1513` 实时解析并渲染
- **影响**：正面增强 — Web版流式体验更好，不阻塞其他操作
- **建议修复**：无需修改

### 差异编号：U-02 调账编辑器交互差异

- **桌面版行为**：Treeview双击编辑，弹出对话框输入调账值，实时计算预览
- **Web版行为**：HTML表格行内编辑，前端 `saveAdjustment()` 计算并提交
- **影响**：低 — 功能等价，交互风格不同
- **建议修复**：无需修改

### 差异编号：U-03 文件操作差异

- **桌面版行为**："打开目录"/"打开文件" 直接打开本地资源管理器
- **Web版行为**：下载链接 `/api/download?path=...` 触发浏览器下载
- **影响**：低 — Web版架构限制，无法直接打开服务器目录
- **建议修复**：无需修改

---

## 五、业务逻辑细节差异

### 差异编号：B-01 删除/修改电路码重叠检测

- **桌面版行为**：合并模块中检测删除列表与修改列表的电路码重叠，重叠项从删除列表中移除，仅执行修改操作
- **Web版行为**：合并通过 `excel_bridge.py` 委托桌面代码，此逻辑已继承；调账模块无此场景
- **影响**：无差异
- **建议修复**：无需修改

### 差异编号：B-02 本地网结算税前收入公式

- **桌面版行为**：税前收入 = `round(差额 / 1.09, 2)`
- **Web版行为**：`adjustment.py:496` 同样 `round(matched_difference / 1.09, 2)`
- **影响**：无差异
- **建议修复**：无需修改

### 差异编号：B-03 差额合计不为0的长春兜底

- **桌面版行为**：汇总表差额合计不为0时，将差额总计加到长春应付款的D列
- **Web版行为**：`adjustment.py` 步骤2中 `first_row['payable'] = round(original_payable + total_adjustment, 2)` 等价实现
- **影响**：无差异
- **建议修复**：无需修改

---

## 六、错误处理差异

### 差异编号：E-01 数据文件预验证缺失

- **桌面版行为**：`validate_data_file()` 在合并前检查源文件必需列（电路代号、数据状态等），`show_validation_results()` 弹窗展示校验报告
- **Web版行为**：无数据内容级别验证，仅有文件级别的存在/大小检查（`_pre_merge_validate()`）
- **影响**：中 — 若源文件缺少必需列，桌面版在合并前即报错，Web版会在合并过程中因 KeyError 崩溃，错误信息不友好
- **建议修复**：在合并前增加源文件列头校验，检查必需列（电路代号等）是否存在

### 差异编号：E-02 工作簿资源清理

- **桌面版行为**：合并流程有完善的 `finally` 块关闭 workbook 对象，清理临时文件
- **Web版行为**：`excel_bridge.py` 委托桌面代码继承此保护；`adjustment.py:617-618` 有基本的 `wb.save()` + `wb.close()`，但异常路径 `except` 块(L630-634)只删除失败文件，未显式关闭 workbook
- **影响**：低 — Python GC 最终会回收，但在高频调用场景可能造成文件句柄泄漏
- **建议修复**：在 `except` 块中增加 `try: wb.close() except: pass`

### 差异编号：E-03 合并输出文件发现机制

- **桌面版行为**：合并完成后通过 `self.current_output_file` 获取输出路径（内存状态）
- **Web版行为**：`excel_bridge.py` 因 mock 导致 `instance.current_output_file` 不可靠，增加了 `_find_latest_output_file()` 扫描输出目录按修改时间查找最新文件
- **影响**：正面增强 — 文件系统扫描比内存状态更可靠
- **建议修复**：无需修改

---

## 汇总统计

| 维度 | 差异数 | 高影响 | 中影响 | 低影响 | 正面增强 |
|------|--------|--------|--------|--------|----------|
| 合并流程 | 6 | 0 | 3 | 2 | 1 |
| 调账流程 | 4 | 1 | 2 | 1 | 0 |
| 数据处理 | 3 | 0 | 0 | 3 | 0 |
| UI/交互 | 3 | 0 | 0 | 3 | 1 |
| 业务逻辑 | 3 | 0 | 0 | 0 | 0 |
| 错误处理 | 3 | 0 | 1 | 2 | 1 |
| **合计** | **22** | **1** | **6** | **11** | **3** |

### 优先修复建议（按影响排序）

1. **A-03** [高] 差额列兜底计算逻辑缺失 — 可能导致调账完全失败
2. **A-04** [中] 年度结算公式单元格被跳过 — 导致部分行数据无法更新
3. **E-01** [中] 数据文件列头预验证缺失 — 错误信息不友好
4. **A-01** [中] 地市匹配策略简化 — 非标准命名可能漏匹配
5. **M-06** [中] .xls格式不支持 — 偶见老格式数据源
6. **M-03** [中] 调账保存前无只读权限修复 — 服务器已有文件可能保存失败
7. **M-01** [中] 月份参数无API层校验 — 直接调API可绕过月份校验

> AI生成